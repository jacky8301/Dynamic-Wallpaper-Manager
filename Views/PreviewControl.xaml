<!-- PreviewControl.xaml -->
<UserControl x:Class="WallpaperEngine.Views.PreviewControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <Style x:Key="PreviewButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="White"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        </Style>
    </UserControl.Resources>
    <Grid>
        <!-- 图片预览 -->
        <Border x:Name="ImagePreview" Visibility="Collapsed" Background="Black">
            <Image x:Name="aPreviewImage" Stretch="Uniform" RenderOptions.BitmapScalingMode="HighQuality">
                <Image.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform x:Name="ImageScale"/>
                        <TranslateTransform x:Name="ImageTranslate"/>
                        <RotateTransform x:Name="ImageRotate"/>
                    </TransformGroup>
                </Image.RenderTransform>
            </Image>
        </Border>

        <!-- 视频预览 -->
        <Border x:Name="VideoPreview" Visibility="Collapsed" Background="Black">
            <MediaElement x:Name="PreviewVideo" LoadedBehavior="Manual" Stretch="Uniform"/>
        </Border>

        <!-- 控制面板 -->
        <StackPanel x:Name="ControlPanel" Orientation="Horizontal" VerticalAlignment="Bottom" 
                   HorizontalAlignment="Center" Margin="10" Background="#CC000000">
            <Button x:Name="PlayPauseBtn" Content="▶" Click="PlayPause_Click" Style="{StaticResource PreviewButtonStyle}"/>
            <Button Content="↺" Click="RotateLeft_Click" Style="{StaticResource PreviewButtonStyle}"/>
            <Button Content="↻" Click="RotateRight_Click" Style="{StaticResource PreviewButtonStyle}"/>
            <Button Content="⤢" Click="FitToWindow_Click" Style="{StaticResource PreviewButtonStyle}"/>
            <Button Content="1:1" Click="ActualSize_Click" Style="{StaticResource PreviewButtonStyle}"/>
        </StackPanel>
    </Grid>
</UserControl>
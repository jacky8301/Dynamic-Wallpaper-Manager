<UserControl
    x:Class="WallpaperEngine.Views.CollectionView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:converters="clr-namespace:WallpaperEngine.Converters"
    xmlns:vwp="clr-namespace:WpfToolkit.Controls;assembly=VirtualizingWrapPanel">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Converters/CommonConverters.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- 左侧：合集列表 -->
        <Border Grid.Column="0" BorderBrush="#E0E0E0" BorderThickness="0,0,1,0" Background="#FAFAFA">
            <DockPanel Cursor="Hand">
                <!-- 顶部按钮 -->
                <StackPanel DockPanel.Dock="Top" Margin="8">
                    <Button
                        Command="{Binding CreateCollectionCommand}"
                        Content="＋ 新建合集"
                        HorizontalAlignment="Stretch"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        materialDesign:ButtonAssist.CornerRadius="4"
                        Margin="0,0,0,8" />
                </StackPanel>

                <!-- 合集列表 -->
                <ListBox
                    ItemsSource="{Binding Collections}"
                    SelectedItem="{Binding SelectedCollection}"
                    BorderThickness="0"
                    Background="Transparent"
                    ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            <Setter Property="Padding" Value="0" />
                            <Setter Property="Margin" Value="0" />
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="12,10" Background="Transparent"
                                    Tag="{Binding DataContext, RelativeSource={RelativeSource AncestorType=UserControl}}">
                                <Border.ContextMenu>
                                    <ContextMenu>
                                        <MenuItem
                                            Header="✏️ 重命名"
                                            Command="{Binding PlacementTarget.Tag.RenameCollectionCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                        <Separator />
                                        <MenuItem
                                            Header="🗑️ 删除合集"
                                            Foreground="Red"
                                            Command="{Binding PlacementTarget.Tag.DeleteCollectionCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                    </ContextMenu>
                                </Border.ContextMenu>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock
                                        Text="{Binding Name}"
                                        FontSize="14"
                                        TextTrimming="CharacterEllipsis" />
                                    <TextBlock
                                        Text="{Binding WallpaperCount, StringFormat=' ({0})'}"
                                        FontSize="14"
                                        Foreground="Gray" />
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>
        </Border>

        <!-- 右侧：壁纸网格 -->
        <Grid Grid.Column="1">
            <!-- 无选中合集时的提示 -->
            <StackPanel
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
                <StackPanel.Style>
                    <Style TargetType="StackPanel">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SelectedCollection}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </StackPanel.Style>
                <TextBlock
                    HorizontalAlignment="Center"
                    FontSize="16"
                    Foreground="#999"
                    Text="请选择或创建一个合集"
                    Margin="0,0,0,16" />
                <Button
                    Command="{Binding CreateCollectionCommand}"
                    Content="＋ 新建合集"
                    HorizontalAlignment="Center"
                    Padding="24,8"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    materialDesign:ButtonAssist.CornerRadius="4" />
            </StackPanel>

            <!-- 壁纸列表 -->
            <ListBox
                ItemsSource="{Binding CollectionWallpapers}"
                BorderThickness="0"
                Background="Transparent"
                ScrollViewer.CanContentScroll="True"
                ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.IsDeferredScrollingEnabled="False"
                ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                VirtualizingPanel.IsVirtualizing="True"
                VirtualizingPanel.VirtualizationMode="Recycling"
                VirtualizingPanel.ScrollUnit="Pixel"
                HorizontalContentAlignment="Stretch">
                <ListBox.Style>
                    <Style TargetType="ListBox">
                        <Setter Property="Visibility" Value="Visible" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SelectedCollection}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ListBox.Style>
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="Margin" Value="0" />
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <vwp:VirtualizingWrapPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border
                            Width="200"
                            Height="150"
                            Margin="5"
                            BorderBrush="#E0E0E0"
                            BorderThickness="1"
                            CornerRadius="4"
                            ClipToBounds="True"
                            Tag="{Binding DataContext, RelativeSource={RelativeSource AncestorType=UserControl}}">
                            <Border.ContextMenu>
                                <ContextMenu>
                                    <MenuItem
                                        Header="👁️ 预览壁纸"
                                        Command="{Binding PlacementTarget.Tag.PreviewWallpaperCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                        CommandParameter="{Binding}" />
                                    <Separator />
                                    <MenuItem
                                        Header="🚀 应用壁纸"
                                        Command="{Binding PlacementTarget.Tag.ApplyWallpaperCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                        CommandParameter="{Binding}" />
                                    <Separator />
                                    <MenuItem
                                        Header="{Binding IsFavorite, Converter={StaticResource FavoriteTextConverter}}"
                                        Command="{Binding PlacementTarget.Tag.ToggleFavoriteCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                        CommandParameter="{Binding}" />
                                    <Separator />
                                    <MenuItem
                                        Header="📁 转到壁纸目录"
                                        Command="{Binding PlacementTarget.Tag.GoToWallpaperDirectoryCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                        CommandParameter="{Binding}" />
                                    <Separator />
                                    <MenuItem Header="📂 添加到合集">
                                        <MenuItem.ItemsSource>
                                            <Binding Path="PlacementTarget.Tag.OtherCollections" RelativeSource="{RelativeSource AncestorType=ContextMenu}" />
                                        </MenuItem.ItemsSource>
                                        <MenuItem.ItemContainerStyle>
                                            <Style TargetType="MenuItem">
                                                <Setter Property="Header">
                                                    <Setter.Value>
                                                        <MultiBinding Converter="{StaticResource CollectionMenuHeaderConverter}">
                                                            <Binding Path="PlacementTarget.DataContext" RelativeSource="{RelativeSource AncestorType=ContextMenu}" />
                                                            <Binding />
                                                        </MultiBinding>
                                                    </Setter.Value>
                                                </Setter>
                                                <Setter Property="Command" Value="{Binding PlacementTarget.Tag.AddToSpecificCollectionCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                                <Setter Property="CommandParameter">
                                                    <Setter.Value>
                                                        <MultiBinding Converter="{StaticResource AddToCollectionConverter}">
                                                            <Binding Path="PlacementTarget.DataContext" RelativeSource="{RelativeSource AncestorType=ContextMenu}" />
                                                            <Binding Path="Id" />
                                                        </MultiBinding>
                                                    </Setter.Value>
                                                </Setter>
                                                <Setter Property="Padding" Value="8,4" />
                                                <Setter Property="Margin" Value="0,2" />
                                            </Style>
                                        </MenuItem.ItemContainerStyle>
                                        <MenuItem.Style>
                                            <Style BasedOn="{StaticResource {x:Type MenuItem}}" TargetType="MenuItem">
                                                <Setter Property="IsEnabled" Value="True" />
                                                <Setter Property="Header" Value="{Binding PlacementTarget.Tag.OtherCollections.Count, RelativeSource={RelativeSource AncestorType=ContextMenu}, Converter={StaticResource CollectionCountConverter}}" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding PlacementTarget.Tag.OtherCollections.Count, RelativeSource={RelativeSource AncestorType=ContextMenu}}" Value="0">
                                                        <Setter Property="IsEnabled" Value="False" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </MenuItem.Style>
                                    </MenuItem>
                                    <Separator />
                                    <MenuItem Header="📦 移动到合集">
                                        <MenuItem.ItemsSource>
                                            <Binding Path="PlacementTarget.Tag.OtherCollections" RelativeSource="{RelativeSource AncestorType=ContextMenu}" />
                                        </MenuItem.ItemsSource>
                                        <MenuItem.ItemContainerStyle>
                                            <Style TargetType="MenuItem">
                                                <Setter Property="Header" Value="{Binding Name}" />
                                                <Setter Property="Command" Value="{Binding PlacementTarget.Tag.MoveToCollectionCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                                                <Setter Property="CommandParameter">
                                                    <Setter.Value>
                                                        <MultiBinding Converter="{StaticResource AddToCollectionConverter}">
                                                            <Binding Path="PlacementTarget.DataContext" RelativeSource="{RelativeSource AncestorType=ContextMenu}" />
                                                            <Binding Path="Id" />
                                                        </MultiBinding>
                                                    </Setter.Value>
                                                </Setter>
                                                <Setter Property="Padding" Value="8,4" />
                                                <Setter Property="Margin" Value="0,2" />
                                            </Style>
                                        </MenuItem.ItemContainerStyle>
                                        <MenuItem.Style>
                                            <Style BasedOn="{StaticResource {x:Type MenuItem}}" TargetType="MenuItem">
                                                <Setter Property="IsEnabled" Value="True" />
                                                <Setter Property="Header" Value="{Binding PlacementTarget.Tag.OtherCollections.Count, RelativeSource={RelativeSource AncestorType=ContextMenu}, Converter={StaticResource CollectionCountConverter}}" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding PlacementTarget.Tag.OtherCollections.Count, RelativeSource={RelativeSource AncestorType=ContextMenu}}" Value="0">
                                                        <Setter Property="IsEnabled" Value="False" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </MenuItem.Style>
                                    </MenuItem>
                                    <Separator />
                                    <MenuItem
                                        Header="🗑️ 从合集移除"
                                        Command="{Binding PlacementTarget.Tag.RemoveFromCollectionCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                        CommandParameter="{Binding}" />
                                    <Separator />
                                    <MenuItem
                                        Header="🗑️ 删除壁纸"
                                        Foreground="Red"
                                        Command="{Binding PlacementTarget.Tag.DeleteWallpaperCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                        CommandParameter="{Binding}" />
                                </ContextMenu>
                            </Border.ContextMenu>
                            <Grid>
                                <Image
                                    Source="{Binding PreviewImagePath, Converter={StaticResource ImageToSourceConverter}, IsAsync=True}"
                                    Stretch="UniformToFill" />
                                <!-- 标题 -->
                                <Border VerticalAlignment="Bottom" Background="#80000000">
                                    <TextBlock
                                        Margin="5"
                                        FontSize="12"
                                        FontWeight="Bold"
                                        Foreground="White"
                                        Height="15"
                                        Text="{Binding Project.Title}"
                                        TextWrapping="Wrap" />
                                </Border>
                                <!-- 移除按钮 -->
                                <Button
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Top"
                                    Margin="4"
                                    Width="24" Height="24"
                                    Padding="0"
                                    ToolTip="从合集移除"
                                    Command="{Binding DataContext.RemoveFromCollectionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}"
                                    Style="{StaticResource MaterialDesignIconForegroundButton}">
                                    <materialDesign:PackIcon Kind="Close" Width="16" Height="16" Foreground="White" />
                                </Button>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </Grid>
</UserControl>

<!-- WallpaperDetailView.xaml -->
<UserControl x:Class="WallpaperEngine.Views.WallpaperDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:converters="clr-namespace:WallpaperEngine.Converters" xmlns:viewmodels="clr-namespace:WallpaperEngine.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:WallpaperDetailViewModel}"
             mc:Ignorable="d">

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="16">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="400"/>
                <RowDefinition Height="200"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" >
                <StackPanel>
                    <TextBlock Text="壁纸详情" FontSize="16" FontWeight="Bold" Background="#F5F5F5" Padding="10"/>
                    <TextBlock Text="{Binding SelectedWallpaper.Project.Title}" FontWeight="Bold" FontSize="14" />
                    <TextBlock Text="{Binding SelectedWallpaper.Project.Description}" 
                                  TextWrapping="Wrap" Margin="0,10" FontSize="12"/>
                    <StackPanel Orientation="Horizontal" Margin="0,5">
                        <TextBlock Text="类型:" FontWeight="Bold" Margin="0,0,5,0"/>
                        <TextBlock Text="{Binding SelectedWallpaper.Project.Type}"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,5">
                        <TextBlock Text="分类:" FontWeight="Bold" Margin="0,0,5,0"/>
                        <TextBlock Text="{Binding SelectedWallpaper.Category}"/>
                    </StackPanel>
                    <TextBlock Text="标签:" FontWeight="Bold" Margin="0,5,0,0"/>
                    <WrapPanel Margin="0,2">
                        <ItemsControl ItemsSource="{Binding SelectedWallpaper.Project.Tags}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#E3F2FD" CornerRadius="10" Margin="2,1" Padding="5,2">
                                        <TextBlock Text="{Binding}" FontSize="10"/>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </WrapPanel>
                </StackPanel>
            </ScrollViewer>

            <!-- 预览区域 -->

            <Border Grid.Row="1" Margin="10" Background="Black" Height="200">
                <!--<Image Source="{Binding SelectedWallpaper.PreviewImagePath, Converter={StaticResource ImageToSourceConverter}}" Stretch="Uniform"/>-->
            </Border>

            <!-- 操作按钮 -->

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
                <!--<Button Content="⭐ 收藏" Click="OnClickFavoriteButton"  Height="30" Margin="5" Foreground="White"/>
                <Button x:Name="btnPreview" Content="👁️ 预览" Click="OnClickPreviewButton" Height="30" Margin="5"/>
                <Button Content="🚀 应用" Click="OnClickApplyWallpaperButton"  Height="30" Margin="5" Background="#4CAF50" Foreground="White"/>
                <Button Content="🗑️ 删除" Click="OnClickDeleteWallpaperButton" Height="30" Margin="5" Background="#F44336" Foreground="White"/>
                <Button Content="🗑️ 编辑" Click="OnClickEditWallpaperButton" Height="30" Margin="5" Background="#F44336" Foreground="White"/>-->
            </StackPanel>
        </Grid>
    </ScrollViewer>
</UserControl>
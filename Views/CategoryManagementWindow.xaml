<Window
    x:Class="WallpaperEngine.Views.CategoryManagementWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    Title="分类管理"
    Width="650"
    Height="500"
    AllowsTransparency="True"
    FontFamily="Segoe UI"
    ResizeMode="CanResize"
    WindowStartupLocation="CenterOwner"
    WindowStyle="None">
    <WindowChrome.WindowChrome>
        <WindowChrome
            CaptionHeight="40"
            GlassFrameThickness="0"
            ResizeBorderThickness="6" />
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Converters/CommonConverters.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  自定义标题栏  -->
        <Border
            Grid.Row="0"
            Height="40"
            Background="{DynamicResource MaterialDesign.Brush.Primary.Light}">
            <Grid>
                <StackPanel
                    Margin="10,0"
                    VerticalAlignment="Center"
                    Orientation="Horizontal">
                    <materialDesign:PackIcon
                        Width="20"
                        Height="20"
                        Margin="0,0,8,0"
                        VerticalAlignment="Center"
                        Foreground="White"
                        Kind="FormatListBulleted" />
                    <TextBlock
                        VerticalAlignment="Center"
                        FontSize="14"
                        FontWeight="SemiBold"
                        Foreground="White"
                        Text="分类管理" />
                </StackPanel>

                <StackPanel
                    HorizontalAlignment="Right"
                    Orientation="Horizontal"
                    WindowChrome.IsHitTestVisibleInChrome="True">
                    <Button
                        x:Name="MinimizeButton"
                        Width="40"
                        Height="40"
                        Click="MinimizeButton_Click"
                        Style="{StaticResource MaterialDesignIconButton}"
                        ToolTip="最小化">
                        <materialDesign:PackIcon
                            Width="16"
                            Height="16"
                            Foreground="White"
                            Kind="WindowMinimize" />
                    </Button>
                    <Button
                        x:Name="MaximizeButton"
                        Width="40"
                        Height="40"
                        Click="MaximizeButton_Click"
                        Style="{StaticResource MaterialDesignIconButton}"
                        ToolTip="最大化">
                        <materialDesign:PackIcon
                            Width="16"
                            Height="16"
                            Foreground="White"
                            Kind="WindowMaximize" />
                    </Button>
                    <Button
                        x:Name="CloseButton"
                        Width="40"
                        Height="40"
                        Click="CloseButton_Click"
                        Style="{StaticResource MaterialDesignIconButton}"
                        ToolTip="关闭">
                        <materialDesign:PackIcon
                            Width="16"
                            Height="16"
                            Foreground="White"
                            Kind="Close" />
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!--  主要内容区域  -->
        <Grid Grid.Row="1" Background="{DynamicResource MaterialDesignPaper}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!--  左侧：分类列表  -->
            <Border Grid.Column="0" BorderBrush="#E0E0E0" BorderThickness="0,0,1,0" Background="#FAFAFA">
                <DockPanel>
                    <!--  顶部统计信息  -->
                    <StackPanel DockPanel.Dock="Top" Margin="12">
                        <TextBlock FontWeight="SemiBold" Text="分类统计" />
                        <TextBlock Margin="0,4,0,0" Text="{Binding TotalCategories, StringFormat='{}{0} 个分类'}" />
                        <TextBlock Text="{Binding TotalWallpapers, StringFormat='{}{0} 个壁纸'}" />
                    </StackPanel>

                    <!--  分类列表  -->
                    <ListBox ItemsSource="{Binding Categories}"
                             SelectedItem="{Binding SelectedCategory}"
                             BorderThickness="0"
                             Background="Transparent"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                <Setter Property="Padding" Value="0" />
                                <Setter Property="Margin" Value="0" />
                                <Setter Property="Background" Value="Transparent" />
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="#E3F2FD" />
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#F5F5F5" />
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="12,10" Background="Transparent">
                                    <StackPanel>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding Name}" FontSize="14" />
                                            <TextBlock Margin="4,0,0,0" FontSize="12" Foreground="#666">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Text" Value="{Binding WallpaperCount, StringFormat='{}({0})'}" />
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsProtected}" Value="True">
                                                                <Setter Property="Foreground" Value="#FF9800" />
                                                                <Setter Property="Text" Value="🔒" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </StackPanel>
                                        <TextBlock Text="{Binding WallpaperCount, StringFormat='{}{0} 个壁纸'}"
                                                   FontSize="12" Foreground="#666" />
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </DockPanel>
            </Border>

            <!--  右侧：操作面板  -->
            <Grid Grid.Column="1" Margin="16">
                <StackPanel>
                    <!--  操作按钮  -->
                    <Button Command="{Binding AddCategoryCommand}"
                            Content="＋ 新增分类"
                            Style="{StaticResource MaterialDesignRaisedButton}"
                            Margin="0,0,0,12" />

                    <Button Command="{Binding RenameCategoryCommand}"
                            Content="✏️ 重命名分类"
                            Style="{StaticResource MaterialDesignOutlinedButton}"
                            Margin="0,0,0,8"
                            IsEnabled="{Binding SelectedCategory.IsProtected, Converter={StaticResource InverseBooleanConverter}}" />

                    <Button Command="{Binding DeleteCategoryCommand}"
                            Content="🗑️ 删除分类"
                            Style="{StaticResource MaterialDesignOutlinedButton}"
                            Foreground="Red"
                            IsEnabled="{Binding SelectedCategory.IsProtected, Converter={StaticResource InverseBooleanConverter}}" />

                    <!--  受保护分类提示  -->
                    <Border Margin="0,24,0,0" Padding="12" Background="#FFF3E0" CornerRadius="4">
                        <TextBlock TextWrapping="Wrap" FontSize="12" Foreground="#E65100">
                            提示：内置分类"所有分类"和"未分类"受保护，不能被重命名或删除。
                        </TextBlock>
                    </Border>

                    <!--  加载状态指示器  -->
                    <Border Margin="0,16,0,0" Padding="12" Background="#F5F5F5" CornerRadius="4"
                            Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <materialDesign:PackIcon
                                Width="16"
                                Height="16"
                                Margin="0,0,8,0"
                                Foreground="#666"
                                Kind="Refresh"
                                RenderTransformOrigin="0.5,0.5">
                                <materialDesign:PackIcon.RenderTransform>
                                    <RotateTransform Angle="0" />
                                </materialDesign:PackIcon.RenderTransform>
                                <materialDesign:PackIcon.Triggers>
                                    <EventTrigger RoutedEvent="Loaded">
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation
                                                    Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                                                    From="0"
                                                    To="360"
                                                    Duration="0:0:1"
                                                    RepeatBehavior="Forever" />
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </EventTrigger>
                                </materialDesign:PackIcon.Triggers>
                            </materialDesign:PackIcon>
                            <TextBlock Text="正在加载分类..." FontSize="12" Foreground="#666" />
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</Window>
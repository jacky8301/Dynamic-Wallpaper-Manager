<Window x:Class="WallpaperEngine.Views.MainWindow"
        x:Name="mainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:WallpaperEngine.ViewModels"
        Title="Wallpaper Engine 管理工具" Height="1240" Width="1650"
        MinHeight="1240" MinWidth="1650"
        xmlns:converters="clr-namespace:WallpaperEngine.Converters"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:views="clr-namespace:WallpaperEngine.Views"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Loaded="mainWindow_Loaded"
    >
    <!-- 设置WindowChrome，定义可拖动区域等 -->
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="40" 
                      ResizeBorderThickness="6"
                      GlassFrameThickness="0"/>
    </WindowChrome.WindowChrome>
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Styles/WallpaperMenuItemStyle.xaml"/>
                <ResourceDictionary Source="/Styles/WallpaperItemContextMenuStyle.xaml"/>
                <ResourceDictionary Source="/Styles/WallpaperItemStyle.xaml"/>
                <ResourceDictionary Source="/Styles/FavoriteButtonStyle.xaml"/>
                <ResourceDictionary Source="/Converters/CommonConverters.xaml"/>
                <ResourceDictionary Source="/Styles/ToolBarStyle.xaml"/>
                <ResourceDictionary Source="/Styles/ModernComboBoxStyle.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid>
        <!-- 你的主界面内容 -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- 标题栏行 -->
            <RowDefinition Height="*"/>
            <!-- 内容行 -->
        </Grid.RowDefinitions>
        <!-- 自定义标题栏 -->
        <views:CustomTitleBar Grid.Row="0" Title="Wallpaper Engine 壁纸管理工具"/>
        <!-- 主内容区域 -->
        <Grid Grid.Row="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <!-- 工具栏 -->
                <views:MyToolBar Grid.Row="0" />
                <!-- 扫描状态显示 -->
                <Border Grid.Row="0" Background="#FFF8E1" Height="30" VerticalAlignment="Bottom" Visibility="{Binding IsScanning, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <StackPanel Orientation="Horizontal" Margin="10,0">
                        <TextBlock Text="{Binding ScanStatus}" FontWeight="Bold" Foreground="{Binding IsScanning}"/>
                        <ProgressBar Value="{Binding ScanProgress}" Width="200" Height="10" IsIndeterminate="{Binding IsIndeterminate}" Margin="20,0"/>
                        <TextBlock Text="{Binding ScannedCount, StringFormat=已扫描: {0} 个}" Margin="10,0" Foreground="Gray"/>
                    </StackPanel>
                 </Border>
                <!-- 内容区域 -->
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="3*"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>
                    <!-- 壁纸网格 -->
                    <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding Wallpapers}" ScrollViewer.IsDeferredScrollingEnabled="True" VirtualizingPanel.IsVirtualizing="True" 
                                      VirtualizingPanel.VirtualizationMode="Recycling">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="#E0E0E0" BorderThickness="1" Margin="5" Width="200" Height="150" Cursor="Hand">
                                        <!-- 右键菜单定义 -->
                                        <Border.ContextMenu>
                                            <ContextMenu Style="{StaticResource WallpaperItemContextMenuStyle}">
                                                <!-- 预览菜单项 -->
                                                <MenuItem Header="👁️ 预览壁纸" Style="{StaticResource WallpaperMenuItemStyle}" Command="{Binding DataContext.PreviewWallpaperCommand,Source={x:Reference mainWindow}}" CommandParameter="{Binding}"/>
                                                <!-- 分隔线 -->
                                                <Separator/>
                                                <!-- 应用菜单项 -->
                                                <MenuItem Header="🚀 应用壁纸" Style="{StaticResource WallpaperMenuItemStyle}" Command="{Binding DataContext.ApplyWallpaperCommand,Source={x:Reference mainWindow}}" CommandParameter="{Binding}"/>
                                                <!-- 分隔线 -->
                                                <Separator/>
                                                <!-- 转到目录菜单项 -->
                                                <MenuItem Header="📁 转到壁纸目录" Style="{StaticResource WallpaperMenuItemStyle}" Command="{Binding DataContext.GoToWallpaperDirectoryCommand,Source={x:Reference mainWindow}}" CommandParameter="{Binding}"/>
                                                <!-- 分隔线 -->
                                                <Separator/>
                                                <!-- 收藏菜单项 -->
                                                <MenuItem Header="⭐️ 收藏壁纸" Style="{StaticResource WallpaperMenuItemStyle}" Command="{Binding DataContext.ToggleFavoriteCommand, Source={x:Reference mainWindow}}" CommandParameter="{Binding}"/>
                                                <!-- 分隔线 -->
                                                <Separator/>
                                                <!-- 删除菜单项 -->
                                                <MenuItem Header="🗑️ 删除壁纸" Style="{StaticResource WallpaperMenuItemStyle}" Command="{Binding DataContext.DeleteWallpaperCommand, Source={x:Reference mainWindow}}" CommandParameter="{Binding}" Foreground="Red"/>
                                            </ContextMenu>
                                        </Border.ContextMenu>
                                        <Border.InputBindings>
                                            <!-- 将左键双击手势绑定到命令 -->
                                            <MouseBinding Gesture="LeftDoubleClick" Command="{Binding DataContext.PreviewWallpaperCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}" />
                                            <!--处理单击事件-->
                                            <MouseBinding Gesture="LeftClick" Command="{Binding DataContext.SelectWallpaperCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}" />
                                        </Border.InputBindings>
                                        <Grid>
                                            <Image Source="{Binding PreviewImagePath, Converter={StaticResource ImageToSourceConverter}}" Stretch="UniformToFill"/>
                                            <Border Background="#80000000" VerticalAlignment="Bottom">
                                                <TextBlock Text="{Binding Project.Title}" Foreground="White" Margin="5" TextWrapping="Wrap" FontWeight="Bold"/>
                                            </Border>
                                            <!-- 收藏按钮 - 固定在右上角 -->
                                            <Button Command="{Binding DataContext.ToggleFavoriteCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                CommandParameter="{Binding}"
                                                HorizontalAlignment="Right" VerticalAlignment="Top"
                                                Margin="5"
                                                Style="{StaticResource FavoriteButtonStyle}">
                                                <!-- 根据 IsFavorite 属性动态显示不同图标 -->
                                                <Button.Content>
                                                    <MultiBinding Converter="{StaticResource FavoriteIconConverter}">
                                                        <Binding Path="IsFavorite"/>
                                                        <Binding Path="IsMouseOver" RelativeSource="{RelativeSource Mode=Self}"/>
                                                    </MultiBinding>
                                                </Button.Content>
                                            </Button>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                    <!-- 详情面板 -->
                    <Border Grid.Column="1" BorderBrush="#E0E0E0" BorderThickness="1,0,0,0">
                        <views:WallpaperDetailView />
                    </Border>
                </Grid>
            </Grid>
        </Grid>
    </Grid>
</Window>
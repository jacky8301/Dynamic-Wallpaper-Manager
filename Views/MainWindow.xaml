<Window
    x:Class="WallpaperEngine.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:WallpaperEngine.Converters"
    xmlns:fa="http://schemas.fontawesome.io/icons/"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:tb="http://www.hardcodet.net/taskbar"
    xmlns:views="clr-namespace:WallpaperEngine.Views"
    xmlns:vm="clr-namespace:WallpaperEngine.ViewModels"
    xmlns:vwp="clr-namespace:WpfToolkit.Controls;assembly=VirtualizingWrapPanel"
    x:Name="mainWindow"
    Title="Wallpaper Engine 管理工具"
    Width="1730"
    Height="1240"
    MinWidth="1730"
    MinHeight="1240"
    AllowsTransparency="True"
    Loaded="mainWindow_Loaded"
    WindowStartupLocation="CenterScreen"
    WindowStyle="None">
    <!--  设置WindowChrome，定义可拖动区域等  -->
    <WindowChrome.WindowChrome>
        <WindowChrome
            CaptionHeight="40"
            GlassFrameThickness="0"
            ResizeBorderThickness="6" />
    </WindowChrome.WindowChrome>
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Styles/WallpaperItemStyle.xaml" />
                <ResourceDictionary Source="/Styles/FavoriteButtonStyle.xaml" />
                <ResourceDictionary Source="/Converters/CommonConverters.xaml" />
                <ResourceDictionary Source="/Styles/ToolBarStyle.xaml" />
                <ResourceDictionary Source="/Styles/ModernComboBoxStyle.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <!--  Tab 切换按钮样式  -->
            <Style x:Key="TabRadioButtonStyle" TargetType="RadioButton">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="Foreground" Value="#666666" />
                <Setter Property="FontSize" Value="13" />
                <Setter Property="Cursor" Value="Hand" />
                <Setter Property="Margin" Value="0" />
                <Setter Property="Padding" Value="16,0" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="RadioButton">
                            <Border
                                x:Name="border"
                                Padding="{TemplateBinding Padding}"
                                Background="{TemplateBinding Background}"
                                BorderBrush="Transparent"
                                BorderThickness="0,0,0,2">
                                <ContentPresenter
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter Property="Foreground" Value="#007ACC" />
                                    <Setter Property="FontWeight" Value="SemiBold" />
                                    <Setter TargetName="border" Property="BorderBrush" Value="#007ACC" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True" />
                                        <Condition Property="IsChecked" Value="False" />
                                    </MultiTrigger.Conditions>
                                    <Setter TargetName="border" Property="Background" Value="#F0F0F0" />
                                    <Setter Property="Foreground" Value="#333333" />
                                </MultiTrigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Window.Resources>
    <materialDesign:DialogHost Identifier="MainRootDialog">
        <Grid>
            <Grid>
                <!--  你的主界面内容  -->
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <!--  标题栏行  -->
                    <RowDefinition Height="*" />
                    <!--  内容行  -->
                </Grid.RowDefinitions>
                <!--  自定义标题栏  -->
                <views:CustomTitleBar Title="Wallpaper Engine 壁纸管理工具" Grid.Row="0" />
                <!--  主内容区域  -->
                <Grid Grid.Row="1">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <!--  工具栏  -->
                        <views:MyToolBar Grid.Row="0" />
                        <!--  Tab 切换栏  -->
                        <Border Grid.Row="1" Background="#FAFAFA" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
                            <StackPanel Orientation="Horizontal" Height="36">
                                <RadioButton Content="全部壁纸" GroupName="MainTab"
                                             IsChecked="{Binding CurrentTab, Converter={StaticResource EqualToZeroConverter}}"
                                             Style="{StaticResource TabRadioButtonStyle}" />
                                <RadioButton Content="我的收藏" GroupName="MainTab"
                                             IsChecked="{Binding CurrentTab, Converter={StaticResource EqualToOneConverter}}"
                                             Style="{StaticResource TabRadioButtonStyle}" />
                                <RadioButton Content="壁纸合集" GroupName="MainTab"
                                             IsChecked="{Binding CurrentTab, Converter={StaticResource EqualToTwoConverter}}"
                                             Style="{StaticResource TabRadioButtonStyle}" />
                            </StackPanel>
                        </Border>
                        <!--  扫描状态显示  -->
                        <Border
                            Grid.Row="0"
                            Height="30"
                            VerticalAlignment="Bottom"
                            Background="#FFF8E1"
                            Visibility="{Binding IsScanning, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <StackPanel Margin="10,0" Orientation="Horizontal">
                                <TextBlock
                                    FontWeight="Bold"
                                    Foreground="{Binding IsScanning}"
                                    Text="{Binding ScanStatus}" />
                                <ProgressBar
                                    Width="200"
                                    Height="10"
                                    Margin="20,0"
                                    IsIndeterminate="{Binding IsIndeterminate}"
                                    Value="{Binding ScanProgress}" />
                                <TextBlock
                                    Margin="10,0"
                                    Foreground="Gray"
                                    Text="{Binding ScannedCount, StringFormat=已扫描: {0} 个}" />
                            </StackPanel>
                        </Border>
                        <!--  内容区域  -->
                        <Grid Grid.Row="2">
                            <!-- Tab 0/1: 壁纸列表 + 详情 -->
                            <Grid>
                                <Grid.Style>
                                    <Style TargetType="Grid">
                                        <Setter Property="Visibility" Value="Visible" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding CurrentTab, Converter={StaticResource EqualToTwoConverter}}" Value="True">
                                                <Setter Property="Visibility" Value="Collapsed" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Grid.Style>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="3*" />
                                    <ColumnDefinition Width="1*" />
                                </Grid.ColumnDefinitions>
                            <!--  壁纸网格  -->
                            <ListBox
                                Grid.Column="0"
                                ItemsSource="{Binding Wallpapers}"
                                ScrollViewer.CanContentScroll="True"
                                ScrollViewer.VerticalScrollBarVisibility="Auto"
                                VirtualizingPanel.IsVirtualizing="True"
                                VirtualizingPanel.VirtualizationMode="Recycling"
                                VirtualizingPanel.ScrollUnit="Pixel"
                                BorderThickness="0"
                                Background="Transparent"
                                HorizontalContentAlignment="Stretch">
                                <ListBox.ItemContainerStyle>
                                    <Style TargetType="ListBoxItem">
                                        <Setter Property="Padding" Value="0"/>
                                        <Setter Property="Margin" Value="0"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="ListBoxItem">
                                                    <ContentPresenter/>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ListBox.ItemContainerStyle>
                                <ListBox.Resources>
                                    <Style x:Key="SelectableBorderStyle" TargetType="Border">
                                        <Setter Property="Background" Value="LightGray" />
                                        <!--  默认背景  -->
                                        <Setter Property="BorderBrush" Value="DarkGray" />
                                        <!--  默认边框  -->
                                        <Setter Property="BorderThickness" Value="1" />
                                        <Setter Property="CornerRadius" Value="5" />
                                        <!--  可选：圆角  -->
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                <Setter Property="Background" Value="Red" />
                                                <!--  选中背景  -->
                                                <Setter Property="BorderBrush" Value="Red" />
                                                <!--  选中边框  -->
                                                <!--  可以添加更多视觉变化，如阴影  -->
                                                <Setter Property="Effect">
                                                    <Setter.Value>
                                                        <DropShadowEffect
                                                            BlurRadius="10"
                                                            ShadowDepth="0"
                                                            Color="Red" />
                                                    </Setter.Value>
                                                </Setter>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </ListBox.Resources>
                                <ListBox.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <vwp:VirtualizingWrapPanel Orientation="Horizontal" />
                                    </ItemsPanelTemplate>
                                </ListBox.ItemsPanel>
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Border
                                            Width="200"
                                            Height="150"
                                            Margin="5"
                                            BorderBrush="#E0E0E0"
                                            BorderThickness="1"
                                            Cursor="Hand"
                                            Style="{StaticResource SelectableBorderStyle}">
                                            <!--  右键菜单定义  -->
                                            <Border.ContextMenu>
                                                <ContextMenu>
                                                    <MenuItem
                                                        Header="👁️ 预览壁纸"
                                                        Command="{Binding DataContext.PreviewWallpaperCommand, Source={x:Reference mainWindow}}"
                                                        CommandParameter="{Binding}" />
                                                    <Separator />
                                                    <MenuItem
                                                        Header="🚀 应用壁纸"
                                                        Command="{Binding DataContext.ApplyWallpaperCommand, Source={x:Reference mainWindow}}"
                                                        CommandParameter="{Binding}" />
                                                    <Separator />
                                                    <MenuItem
                                                        Header="{Binding IsFavorite, Converter={StaticResource FavoriteTextConverter}}"
                                                        Command="{Binding DataContext.ToggleFavoriteCommand, Source={x:Reference mainWindow}}"
                                                        CommandParameter="{Binding}" />
                                                    <Separator />
                                                    <MenuItem
                                                        Header="📁 转到壁纸目录"
                                                        Command="{Binding DataContext.GoToWallpaperDirectoryCommand, Source={x:Reference mainWindow}}"
                                                        CommandParameter="{Binding}" />
                                                    <Separator />
                                                    <MenuItem
                                                        Header="📂 添加到合集"
                                                        Command="{Binding DataContext.AddToCollectionCommand, Source={x:Reference mainWindow}}"
                                                        CommandParameter="{Binding}" />
                                                    <Separator />
                                                    <MenuItem
                                                        Header="🗑️ 删除壁纸"
                                                        Foreground="Red"
                                                        Command="{Binding DataContext.DeleteWallpaperCommand, Source={x:Reference mainWindow}}"
                                                        CommandParameter="{Binding}" />
                                                </ContextMenu>
                                            </Border.ContextMenu>
                                            <Border.InputBindings>
                                                <!--  将左键双击手势绑定到命令  -->
                                                <MouseBinding
                                                    Command="{Binding DataContext.PreviewWallpaperCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                    CommandParameter="{Binding}"
                                                    Gesture="LeftDoubleClick" />
                                                <!--  处理单击事件  -->
                                                <MouseBinding
                                                    Command="{Binding DataContext.SelectWallpaperCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                    CommandParameter="{Binding}"
                                                    Gesture="LeftClick" />
                                            </Border.InputBindings>
                                            <Grid>
                                                <Image Source="{Binding PreviewImagePath, Converter={StaticResource ImageToSourceConverter}, IsAsync=True}" Stretch="UniformToFill" />
                                                <Border VerticalAlignment="Bottom" Background="#80000000">
                                                    <TextBlock
                                                        Margin="5"
                                                        FontWeight="Bold"
                                                        Foreground="White"
                                                        Text="{Binding Project.Title, Mode=TwoWay}"
                                                        TextWrapping="Wrap" />
                                                </Border>
                                                <!--  收藏按钮 - 固定在右上角  -->
                                                <Button
                                                    Margin="5"
                                                    HorizontalAlignment="Right"
                                                    VerticalAlignment="Top"
                                                    Command="{Binding DataContext.ToggleFavoriteCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                    CommandParameter="{Binding}"
                                                    Style="{StaticResource FavoriteButtonStyle}">
                                                    <!--  根据 IsFavorite 属性动态显示不同图标  -->
                                                    <Button.Content>
                                                        <MultiBinding Converter="{StaticResource FavoriteIconConverter}">
                                                            <Binding Path="IsFavorite" />
                                                            <Binding Path="IsMouseOver" RelativeSource="{RelativeSource Mode=Self}" />
                                                        </MultiBinding>
                                                    </Button.Content>
                                                </Button>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                            <!--  详情面板  -->
                            <Border
                                Grid.Column="1"
                                BorderBrush="#E0E0E0"
                                BorderThickness="1,0,0,0">
                                <views:WallpaperDetailView />
                            </Border>
                            </Grid>
                            <!-- Tab 2: 壁纸合集 -->
                            <views:CollectionView x:Name="CollectionViewPanel">
                                <views:CollectionView.Style>
                                    <Style TargetType="views:CollectionView">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding DataContext.CurrentTab, ElementName=mainWindow, Converter={StaticResource EqualToTwoConverter}}" Value="True">
                                                <Setter Property="Visibility" Value="Visible" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </views:CollectionView.Style>
                            </views:CollectionView>
                        </Grid>
                    </Grid>
                </Grid>

            </Grid>
            <!--  (2) 加载叠加层  -->
            <Grid
                x:Name="LoadingOverlay"
                Margin="0,40"
                Panel.ZIndex="100"
                Background="#E5111111"
                IsHitTestVisible="True"
                Visibility="Visible">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <!--  进度条  -->
                    <ProgressBar
                        Width="200"
                        Height="10"
                        Margin="0,0,0,20"
                        Foreground="#FF4A90E2"
                        IsIndeterminate="True" />
                    <!--  加载文本  -->
                    <TextBlock
                        HorizontalAlignment="Center"
                        FontSize="14"
                        Foreground="White"
                        Text="正在加载，请稍候..." />
                </StackPanel>
            </Grid>
            <!--  系统托盘图标  -->
            <tb:TaskbarIcon
                x:Name="TrayIcon"
                IconSource="/Assets/app.ico"
                TrayMouseDoubleClick="TrayIcon_TrayMouseDoubleClick"
                ToolTipText="Dynamic Wallpaper Manager">
                <tb:TaskbarIcon.ContextMenu>
                    <ContextMenu>
                        <MenuItem Click="ShowWindow_Click" Header="显示窗口" />
                        <Separator />
                        <MenuItem Click="ExitApp_Click" Header="退出" />
                    </ContextMenu>
                </tb:TaskbarIcon.ContextMenu>
            </tb:TaskbarIcon>
        </Grid>
    </materialDesign:DialogHost>
</Window>